<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.devoir3">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Devoir3">
        <activity android:name=".InfoPerso"></activity>
        <activity android:name=".MonProfil" />
        <activity android:name=".RechercherCours" />
        <activity android:name=".MesCours">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DetailSeance" />
        <activity android:name=".CoursInterieur" />
    </application>

</manifest>